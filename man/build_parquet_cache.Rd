% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parq_support.R
\name{build_parquet_cache}
\alias{build_parquet_cache}
\title{Populate the local parquet cache from a semsql SQLite file}
\usage{
build_parquet_cache(
  sqlite_path = NULL,
  ontology = "go",
  out_dir = NULL,
  tables = c("statements", "entailed_edge", "term_association")
)
}
\arguments{
\item{sqlite_path}{character scalar path to the semsql SQLite file.
If \code{NULL} (default), retrieved via \code{ontoProc2::semsql_connect()}.}

\item{ontology}{character scalar. Default \code{"go"}.}

\item{out_dir}{path to desired folder for parquet cache management}

\item{tables}{character vector of table names to convert. Default covers
the two tables the package actively queries.}
}
\value{
the parquet cache directory path, invisibly.
}
\description{
Converts the semsql SQLite tables to parquet format and writes them
to the BiocFileCache-managed parquet directory.  This is a one-time
setup step - subsequent calls to \code{make_go_con(backend = "parquet")}
or \code{make_go_con(backend = "auto")} will use the cached files
without re-converting.
}
\examples{
if (!has_parquet_cache()) {
  build_parquet_cache()
}

}
